<html>

    <body>
        <div>
            <h1>Your Dashboard</h1>
        </div>
        <h2>Subscribed artists</h2>
        <!-- <div><%artists%></div> -->
        <div id = "main">
        <% if (artists.length) { %>
            <ul id = "sub">
                <% artists.forEach(function(artist) { %>
                <img src = '<%= artist.artist_img %>'></img><br>
                    <%= artist.artist_name %><br>
                <% }); %>
            </ul>
          <% } else { %>
            <p>No subscribed artists found.</p>
          <% } %>
        <h2> Notifications</h2>
        <% if (notifications.length) { %>
            <ul>
            <% notifications.forEach(function(notification) { %>
                <%= notification.notification %><br>
            <% }); %>
            </ul>
          <% } else { %>
            <p>No notifications found.</p>
          <% } %>
        </div>
        <div>
            <h2> Artist Search</h2>
            <button id = "search-page">Search For Artist</button>
        </div>
        <div>User settings and Key page
            <button id = "user-settings">User Settings</button>
        </div>
    </body>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
    <script src = "userprofilepage.js"></script>
    <link href="userprofilepage.css" rel="stylesheet" type="text/css">
</html>